import styles from "./styles.module.scss";

const Files = () => {
  return (
    <ul className={styles.container}>
      <li>
        Тут можна буде зберігати файли і потім надавати доступ до них учням
      </li>
    </ul>
  );
};

export default Files;
