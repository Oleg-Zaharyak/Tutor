import styles from "./styles.module.scss";

const Quizzes = () => {
  return (
    <ul className={styles.container}>
      <li>
        Тут буде можна створвати якісь тести, завдання, ігри і може ще щось
      </li>
    </ul>
  );
};

export default Quizzes;
